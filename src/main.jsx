import React from 'react';
import { createRoot } from 'react-dom/client';
import SecondarySchoolPage from './SecondarySchoolPage.jsx';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SecondarySchoolPage />
  </React.StrictMode>,
);
